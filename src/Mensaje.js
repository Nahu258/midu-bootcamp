const Mensaje = (props) => {
    return <h1 style={{color: props.color}}>
        {props.mensaje}
        </h1>
}

// const Suma = (a, b) => {
//     return (a+b)
// }

export default Mensaje